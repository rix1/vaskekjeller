{
  "name": "vaskekjeller",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "wrangler d1 migrations apply vaskekjeller --local && wrangler dev",
    "deploy": "CI=true wrangler d1 migrations apply vaskekjeller --remote && wrangler deploy",
    "types": "wrangler types",
    "typecheck": "tsc --noEmit && tsc --noEmit -p scripts && tsc --noEmit -p client && tsc --noEmit -p client/tsconfig.sw.json",
    "db:migrate:local": "wrangler d1 migrations apply vaskekjeller --local",
    "db:migrate:remote": "wrangler d1 migrations apply vaskekjeller --remote",
    "vapid": "node scripts/gen-vapid.ts",
    "tenant:create": "node scripts/create-tenant.ts",
    "demo:seed": "node scripts/seed-demo.ts",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "hono": "^4.13.8"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "typescript": "^7.0.2",
    "wrangler": "^4.136.3",
    "esbuild": "^0.28.1"
  }
}
